<nav class="nav  sticky-top navbar-dark bg-ligh d-flex justify-content-between">
    <a class="p-2 text-muted" href="{{path('app_home')}}">
        Home
    </a>
    <div class="col-4 pt-1">
            {% if  is_granted('ROLE_ADMIN') %}
                <a class="text-muted" href="{{path('app_admin_dashboard')}}">Dashboard</a>
            {% endif %}
    </div>
    <div class="col-4 pt-1">
            {% if  is_granted('ROLE_USER') %}
                <a class="text-muted" href="{{path('app_user_show')}}">Profile</a>
            {% endif %}
    </div>
    <a class="p-2 text-muted" href="#">
        About
    </a>
    <a class="p-2 text-muted" href="#">
        Contact
    </a>
</nav>



        